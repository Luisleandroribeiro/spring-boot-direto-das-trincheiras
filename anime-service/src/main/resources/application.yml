spring:
  application:
    name: anime-service
  threads:
    virtual:
      enable: true

logging:
  level:
    academy.devdojo: debug
server:
  serverlet:
    context-path: /
  error:
    include-stacktrace: on_param
  tomcate:
    max: 20